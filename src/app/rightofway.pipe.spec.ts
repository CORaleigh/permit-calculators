import { RightofwayPipe } from './rightofway.pipe';

describe('RightofwayPipe', () => {
  it('create an instance', () => {
    const pipe = new RightofwayPipe();
    expect(pipe).toBeTruthy();
  });
});
