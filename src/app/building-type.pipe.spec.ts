import { BuildingTypePipe } from './building-type.pipe';

describe('BuildingTypePipe', () => {
  it('create an instance', () => {
    const pipe = new BuildingTypePipe();
    expect(pipe).toBeTruthy();
  });
});
