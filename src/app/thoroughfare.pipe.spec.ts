import { ThoroughfarePipe } from './thoroughfare.pipe';

describe('ThoroughfarePipe', () => {
  it('create an instance', () => {
    const pipe = new ThoroughfarePipe();
    expect(pipe).toBeTruthy();
  });
});
