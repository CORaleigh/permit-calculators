import { ConstructionTypePipe } from './construction-type.pipe';

describe('ConstructionTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ConstructionTypePipe();
    expect(pipe).toBeTruthy();
  });
});
